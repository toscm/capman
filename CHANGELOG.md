# 0.2.1-dev

- macOS: Always switch to insert mode when triggering 'Find Anything' (`CapsLock+f`).
- macOS: Add VS Code-specific `CapsLock+g` mapping to send `Option+G` and switch to insert mode.
- macOS: Drop `control` from the optional modifiers for movement keys `i/j/k/l`.
- macOS: Bind `Option+Command+I` to `Option+Command+Up Arrow` in Control Mode (to allow generation of new cursors in VSCode).
- macOS: Bind `Option+Command+K` to `Option+Command+Down Arrow` in Control Mode.
